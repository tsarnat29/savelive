import React from "react";
import CreateUnit from "../components/modals/CreateUnit";
import { observer } from "mobx-react-lite";
const SecUnit = observer(() => {
  return <CreateUnit />;
});

export default SecUnit;
